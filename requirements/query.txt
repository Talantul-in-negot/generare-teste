-r base.txt

# ── Reranking + GNN ────────────────────────────────────────────────────────────
# The query image is CPU-only.  Without this explicit wheel, current PyPI torch
# releases pull a multi-gigabyte CUDA toolkit into the production image.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
sentence-transformers>=3.0.0,<4.0.0
networkx>=3.3

# ── RDF / Ontology ─────────────────────────────────────────────────────────────
rdflib>=7.0.0
owlrl>=6.0.0

# ── Messaging ──────────────────────────────────────────────────────────────────
pika>=1.3.2
