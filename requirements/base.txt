# ── Shared by all services ─────────────────────────────────────────────────────
redis[asyncio]>=5.0.0
neo4j>=5.20.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
structlog>=24.2.0
tenacity>=8.3.0
aio-pika>=9.4.0
# Worker readiness probes and the provider clients used by the shared LLM
# adapter.  Keep these in the common layer: every worker imports the health
# server and may select a provider via environment configuration.
aiohttp>=3.9.0
groq>=0.9.0
openai>=1.30.0
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
